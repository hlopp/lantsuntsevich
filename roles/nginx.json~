{
  "name": "nginx",
  "chef_type": "role",
  "json_class": "Chef::Role",
  "default_attributes": {
  },
  "description": "install nginx",
  "run_list": [
    "recipe[web::install]"
  ],
  "env_run_lists" : {
    "chef_dev": [
        "recipe[jboss::default]",
        "recipe[jboss::deploy]"
    ]
   }
}
